import React from 'react';

import { LoaderWrapper, Spinner } from './styled';

function Loader() {
  return (
    <LoaderWrapper>
      <Spinner />
    </LoaderWrapper>
  );
}

export default Loader;
